[package]
name = "adrakestory"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.15"
serde = { version = "1.0", features = ["derive"] }
ron = "0.8"
thiserror = "1.0"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3